# Для локального запуска src/ и ноутбука-отчёта GPU не нужен,
# но bitsandbytes требует CUDA — ставится только на GPU-машине.
#
# Верхние границы важны: код писался под trl 0.12 + transformers 4.46
# + datasets 3.x; transformers 5.x и trl 1.x ломают API DPOTrainer,
# huggingface_hub 1.x — несовместимые переименования, datasets 4.x —
# другой lazy-loader. На remote/Colab T4 мы воспроизводимо гоняем
# в пределах этих диапазонов.
torch>=2.3,<2.6
transformers>=4.45,<5.0
# accelerate >= 1.3 нужен из-за keep_torch_compile в Accelerator.unwrap_model:
# transformers >= 4.49 его уже зовёт, а accelerate < 1.3 не имеет — Trainer
# падает TypeError при первом шаге train(). Гоняли на 1.13.0.
accelerate>=1.3,<2
peft>=0.13,<0.16
bitsandbytes>=0.43,<0.46
datasets>=2.20,<4.0
trl>=0.12,<1.0
huggingface_hub>=0.25,<1.0
safetensors>=0.4,<1.0

# Данные, анализ, отчёт
pandas
numpy
matplotlib
tqdm
regex
nbformat
jupyter
