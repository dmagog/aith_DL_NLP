# Для локального запуска src/ и ноутбука-отчёта GPU не нужен,
# но bitsandbytes требует CUDA — ставится только на Colab-машине.
torch>=2.3
transformers>=4.45
accelerate>=0.33
peft>=0.13
bitsandbytes>=0.43
datasets>=2.20
trl>=0.11
huggingface_hub>=0.25
safetensors>=0.4

# Данные, анализ, отчёт
pandas
numpy
matplotlib
tqdm
regex
nbformat
jupyter
